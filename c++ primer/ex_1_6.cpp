/* std::cout << "The sum of " << v1; 
    << " and " << v2;
    << " is " << v1 + v2 << std::endl; */

//Program fragment is not legal because the << operator is missing it's left hand opperand on lines 2 and 3